<?xml version="1.0" encoding="utf-8"?>
<menu   xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        tools:showIn="navigation_view"
>
    <group
            android:checkableBehavior="single"
    >
        <item
                android:id="@+id/home"
                android:icon="@drawable/home_icon"
                android:title="Tổng quát"
        />
        <item
                android:id="@+id/employee"
                android:icon="@drawable/person_icon"
                android:title="Nhân viên"
        />
        <item
                android:id="@+id/film"
                android:icon="@drawable/baseline_movie_filter_24"
                android:title="Phim"
        />
        <item
                android:id="@+id/typefilm"
                android:icon="@drawable/ic_type_film"
                android:title="Loại phim"
        />
        <item
            android:id="@+id/bills"
            android:icon="@drawable/ic_bill"
            android:title="Hóa đơn" />
        <item
            android:id="@+id/dishes"
            android:icon="@drawable/ic_dish"
            android:title="Món ăn" />
    </group>

    <item>
        <menu>
            <item android:id="@+id/logout"
                  android:icon="@drawable/baseline_logout_24"
                  android:title="Logout"
            />
        </menu>
    </item>
</menu>